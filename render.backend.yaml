services:
  - type: web
    name: retro-vault-backend
    runtime: docker
    dockerfilePath: ./Dockerfile.backend
    plan: free
    envVars:
      - key: PORT
        value: 8787
      - key: DATA_DIR
        value: server/data
      - key: CORS_ORIGIN
        sync: false
